{
  "openapi": "3.0.3",
  "info": {
    "title": "Friend Group Member Resource Schema",
    "description": "FriendGroupMemberResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "FriendGroupMemberResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "FriendGroupMember"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.name must be friend_group_id:peer_public_key; only friend_group_id is a custom ID."
          },
          "spec": {
            "$ref": "#/components/schemas/FriendGroupMemberSpec"
          }
        }
      },
      "FriendGroupMemberSpec": {
        "type": "object",
        "required": [
          "friend_group_id",
          "peer_public_key",
          "role"
        ],
        "properties": {
          "friend_group_id": {
            "type": "string",
            "minLength": 8,
            "maxLength": 48,
            "pattern": "^[a-z][a-z0-9._-]{6,46}[a-z0-9]$",
            "description": "Friend group custom ID. The peer_public_key segment is not a custom ID."
          },
          "peer_public_key": {
            "type": "string"
          },
          "role": {
            "$ref": "#/components/schemas/FriendGroupMemberRole"
          }
        }
      },
      "FriendGroupMemberRole": {
        "type": "string",
        "enum": [
          "owner",
          "admin",
          "member"
        ]
      }
    }
  }
}
