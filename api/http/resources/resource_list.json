{
  "openapi": "3.0.3",
  "info": {
    "title": "Resource List Resource Schema",
    "description": "ResourceListResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "ResourceListSpec": {
        "type": "object",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "description": "Concrete resources only; nested ResourceList envelopes are invalid.",
            "items": {
              "$ref": "./resource.json#/components/schemas/ConcreteResource"
            }
          }
        }
      },
      "ResourceListResource": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "apiVersion",
          "kind",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "ResourceList"
            ]
          },
          "spec": {
            "$ref": "#/components/schemas/ResourceListSpec"
          }
        }
      }
    }
  }
}
