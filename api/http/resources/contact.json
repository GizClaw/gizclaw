{
  "openapi": "3.0.3",
  "info": {
    "title": "Contact Resource Schema",
    "description": "ContactResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "ContactResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "Contact"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.name is the immutable Peer-visible contact name; metadata.id is omitted on create and required on update."
          },
          "spec": {
            "$ref": "#/components/schemas/ContactSpec"
          }
        }
      },
      "ContactSpec": {
        "type": "object",
        "required": [
          "owner_public_key"
        ],
        "properties": {
          "owner_public_key": {
            "type": "string",
            "minLength": 1,
            "description": "Immutable owner peer public key."
          },
          "display_name": {
            "type": "string"
          },
          "phone_number": {
            "type": "string"
          }
        }
      }
    }
  }
}
