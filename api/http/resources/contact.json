{
  "openapi": "3.0.3",
  "info": {
    "title": "Contact Resource Schema",
    "description": "ContactResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "ContactResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "Contact"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.name must be owner_public_key:id; only id is a custom ID."
          },
          "spec": {
            "$ref": "#/components/schemas/ContactSpec"
          }
        }
      },
      "ContactSpec": {
        "type": "object",
        "required": [
          "owner_public_key",
          "id"
        ],
        "properties": {
          "owner_public_key": {
            "type": "string",
            "minLength": 1,
            "description": "Owner peer public key. metadata.name must be owner_public_key:id."
          },
          "id": {
            "type": "string",
            "minLength": 8,
            "maxLength": 48,
            "pattern": "^[a-z][a-z0-9._-]{6,46}[a-z0-9]$",
            "description": "Owner-scoped contact custom ID. metadata.name must be owner_public_key:id."
          },
          "display_name": {
            "type": "string"
          },
          "phone_number": {
            "type": "string"
          }
        }
      }
    }
  }
}
