{
  "openapi": "3.0.3",
  "info": {
    "title": "Workspace Resource Schema",
    "description": "WorkspaceResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "WorkspaceResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "Workspace"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.id is the canonical Workspace ID when updating, metadata.name is the immutable Workspace name, and spec.workflow_id is the canonical Workflow ID."
          },
          "spec": {
            "$ref": "../shared/workspace_spec.json#/components/schemas/WorkspaceSpec"
          },
          "icon": {
            "$ref": "../shared/icon.json#/components/schemas/Icon"
          }
        }
      }
    }
  }
}
