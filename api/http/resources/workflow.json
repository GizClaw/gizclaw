{
  "openapi": "3.0.3",
  "info": {
    "title": "Workflow Resource Schema",
    "description": "WorkflowResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "WorkflowResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "Workflow"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.id is the caller-supplied canonical Workflow ID. RuntimeProfile binding aliases remain the separate Peer-visible selectors."
          },
          "spec": {
            "$ref": "../shared/workflow_spec.json#/components/schemas/WorkflowSpec"
          }
        }
      }
    }
  }
}
