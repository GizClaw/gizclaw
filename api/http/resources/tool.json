{
  "openapi": "3.0.3",
  "info": {
    "title": "Tool Resource Schema",
    "description": "ToolResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "ToolResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "Tool"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.id is the caller-supplied canonical Tool ID. spec.invoke_name is the distinct immutable runtime execution name."
          },
          "spec": {
            "$ref": "../shared/tool_spec.json#/components/schemas/ToolSpec"
          }
        }
      }
    }
  }
}
