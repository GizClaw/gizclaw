{
  "openapi": "3.0.3",
  "info": {
    "title": "Friend Group Resource Schema",
    "description": "FriendGroupResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "FriendGroupResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "FriendGroup"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.id is the caller-supplied canonical FriendGroup ID. spec.name is the immutable owner-scoped Peer-visible group name."
          },
          "spec": {
            "$ref": "#/components/schemas/FriendGroupSpec"
          }
        }
      },
      "FriendGroupSpec": {
        "type": "object",
        "required": [
          "owner_public_key",
          "name"
        ],
        "properties": {
          "owner_public_key": {
            "type": "string",
            "minLength": 1,
            "description": "Immutable owner Peer public key used to resolve the system Workflow."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Immutable owner-scoped Peer-visible friend group name."
          },
          "description": {
            "type": "string"
          },
          "display_name": {
            "type": "string"
          }
        }
      }
    }
  }
}
