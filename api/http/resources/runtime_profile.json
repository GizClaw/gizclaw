{
  "openapi": "3.0.3",
  "info": {
    "title": "RuntimeProfile Resource Schema",
    "description": "RuntimeProfile declarative resource envelope.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "RuntimeProfileResource": {
        "type": "object",
        "required": ["apiVersion", "kind", "metadata", "spec"],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": ["RuntimeProfile"]
          },
          "metadata": {
            "$ref": "./resource.json#/components/schemas/ResourceMetadata"
          },
          "spec": {
            "$ref": "../shared/runtime_profile_spec.json#/components/schemas/RuntimeProfileSpec"
          }
        }
      }
    }
  }
}
