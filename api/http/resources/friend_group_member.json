{
  "openapi": "3.0.3",
  "info": {
    "title": "Friend Group Member Resource Schema",
    "description": "FriendGroupMemberResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "FriendGroupMemberResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "FriendGroupMember"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.id must equal the deterministic membership ID. spec.name is this member's immutable Peer-visible group name."
          },
          "spec": {
            "$ref": "#/components/schemas/FriendGroupMemberSpec"
          }
        }
      },
      "FriendGroupMemberSpec": {
        "type": "object",
        "required": [
          "friend_group_id",
          "peer_public_key",
          "name",
          "role"
        ],
        "properties": {
          "friend_group_id": {
            "type": "string",
            "minLength": 1,
            "description": "Caller-supplied canonical FriendGroup ID. The peer_public_key segment is not a resource ID."
          },
          "peer_public_key": {
            "type": "string"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Immutable member-visible owner-scoped friend group name."
          },
          "role": {
            "$ref": "#/components/schemas/FriendGroupMemberRole"
          }
        }
      },
      "FriendGroupMemberRole": {
        "type": "string",
        "enum": [
          "owner",
          "admin",
          "member"
        ]
      }
    }
  }
}
