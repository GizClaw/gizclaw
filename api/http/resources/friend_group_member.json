{
  "openapi": "3.0.3",
  "info": {
    "title": "Friend Group Member Resource Schema",
    "description": "FriendGroupMemberResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "FriendGroupMemberResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "FriendGroupMember"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.name is this member's immutable Peer-visible group name; metadata.id is the canonical membership ID returned by the Server."
          },
          "spec": {
            "$ref": "#/components/schemas/FriendGroupMemberSpec"
          }
        }
      },
      "FriendGroupMemberSpec": {
        "type": "object",
        "required": [
          "friend_group_id",
          "peer_public_key",
          "role"
        ],
        "properties": {
          "friend_group_id": {
            "type": "string",
            "minLength": 1,
            "description": "Opaque canonical FriendGroup ID returned by the Server. The peer_public_key segment is not a resource ID."
          },
          "peer_public_key": {
            "type": "string"
          },
          "role": {
            "$ref": "#/components/schemas/FriendGroupMemberRole"
          }
        }
      },
      "FriendGroupMemberRole": {
        "type": "string",
        "enum": [
          "owner",
          "admin",
          "member"
        ]
      }
    }
  }
}
