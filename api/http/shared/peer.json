{
  "openapi": "3.0.3",
  "info": {
    "title": "Peer Type",
    "description": "Peer schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "Peer": {
        "type": "object",
        "required": [
          "public_key",
          "role",
          "status",
          "device",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "public_key": {
            "type": "string"
          },
          "role": {
            "$ref": "./peer_role.json#/components/schemas/PeerRole"
          },
          "status": {
            "$ref": "./peer_registration_status.json#/components/schemas/PeerRegistrationStatus"
          },
          "device": {
            "$ref": "./device_info.json#/components/schemas/DeviceInfo"
          },
          "auto_registered": {
            "type": "boolean"
          },
          "firmware_id": {
            "type": "string",
            "description": "Optional Server-assigned Firmware release-line ID. Channel selection remains device-owned."
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          },
          "approved_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}
