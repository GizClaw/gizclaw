{
  "openapi": "3.0.3",
  "info": {
    "title": "Workflow I18n Type",
    "description": "Workflow-owned localized display text.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "WorkflowI18nCatalog": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          }
        }
      },
      "WorkflowI18n": {
        "type": "object",
        "description": "Workflow-owned locale catalogs. default_locale must name one of the locale catalog properties.",
        "minProperties": 2,
        "required": [
          "default_locale"
        ],
        "properties": {
          "default_locale": {
            "type": "string",
            "description": "Locale key used when the requested locale is unavailable. A catalog with this key is required."
          }
        },
        "additionalProperties": {
          "$ref": "#/components/schemas/WorkflowI18nCatalog"
        }
      }
    }
  }
}
