{
  "openapi": "3.0.3",
  "info": {
    "title": "Device Identifiers Type",
    "description": "DeviceIdentifiers schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "DeviceIdentifiers": {
        "type": "object",
        "properties": {
          "sn": {
            "type": "string",
            "description": "Optional client-declared serial number. Clients should keep it stable and unique per physical device, but servers must tolerate duplicates.",
            "maxLength": 256
          },
          "imeis": {
            "type": "array",
            "items": {
              "$ref": "./peer_imei.json#/components/schemas/PeerIMEI"
            }
          },
          "labels": {
            "type": "array",
            "items": {
              "$ref": "./peer_label.json#/components/schemas/PeerLabel"
            }
          }
        }
      }
    }
  }
}
