{
  "openapi": "3.0.3",
  "info": {
    "title": "Firmware Type",
    "description": "Declarative Firmware channel schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "Firmware": {
        "type": "object",
        "required": [
          "id",
          "slots",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "description": {
            "type": "string",
            "example": "Devkit firmware channels"
          },
          "slots": {
            "$ref": "./firmware_slots.json#/components/schemas/FirmwareSlots"
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}
