{
  "openapi": "3.0.0",
  "info": { "title": "GizClaw Workflow Voice Adapter", "version": "1.0.0" },
  "paths": {},
  "components": {
    "schemas": {
      "VoiceAdapter": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "asr_model": { "type": "string", "maxLength": 63, "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*(?:\\.[a-z0-9]+(?:-[a-z0-9]+)*)*$" },
          "default_voice": { "type": "string", "maxLength": 63, "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*(?:\\.[a-z0-9]+(?:-[a-z0-9]+)*)*$" },
          "node_voices": { "type": "object", "additionalProperties": { "type": "string", "maxLength": 63, "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*(?:\\.[a-z0-9]+(?:-[a-z0-9]+)*)*$" } }
        }
      }
    }
  }
}
