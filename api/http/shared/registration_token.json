{
  "openapi": "3.0.3",
  "info": {
    "title": "RegistrationToken API Types",
    "description": "RegistrationToken persisted resource.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "RegistrationToken": {
        "type": "object",
        "required": ["id", "token", "runtime_profile_id", "created_at", "updated_at"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string" },
          "token": { "type": "string" },
          "runtime_profile_id": { "type": "string" },
          "firmware_id": {
            "type": "string",
            "description": "Optional caller-defined Firmware release-line ID. The device selects its own channel."
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}
