{
  "openapi": "3.0.3",
  "info": {
    "title": "Firmware Slot Type",
    "description": "Firmware slot schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "FirmwareSlot": {
        "type": "object",
        "properties": {
          "description": {
            "type": "string",
            "maxLength": 1024,
            "example": "Devkit firmware 1.0.3"
          },
          "package": {
            "$ref": "./firmware_package.json#/components/schemas/FirmwarePackage",
            "description": "Operator-owned external .tar.zlib package configuration for this channel."
          }
        }
      }
    }
  }
}
