{
  "openapi": "3.0.3",
  "info": {
    "title": "Workflow Type",
    "description": "Canonical persisted Workflow schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "Workflow": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "spec"
        ],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "description": "Immutable opaque canonical ID supplied by the caller."
          },
          "spec": {
            "$ref": "./workflow_spec.json#/components/schemas/WorkflowSpec"
          }
        }
      }
    }
  }
}
