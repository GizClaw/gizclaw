{
  "openapi": "3.0.3",
  "info": {
    "title": "Workflow Type",
    "description": "Canonical persisted Workflow schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "Workflow": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "name",
          "spec"
        ],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "description": "Immutable opaque canonical ID allocated by the Server."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Immutable workflow name used by Peer RPC projections.",
            "x-go-type-skip-optional-pointer": true,
            "example": "demo-assistant"
          },
          "spec": {
            "$ref": "./workflow_spec.json#/components/schemas/WorkflowSpec"
          }
        }
      }
    }
  }
}
